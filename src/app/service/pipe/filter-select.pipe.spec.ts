import { FilterSelectPipe } from './filter-select.pipe';

describe('FilterSelectPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterSelectPipe();
    expect(pipe).toBeTruthy();
  });
});
